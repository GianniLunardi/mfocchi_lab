# robot_control of Locosim for ANYmal D peering

Use this repo (branch `peering`) instead of the submodule in locosim (temp solution).

## Simulation
```
python3 base_controllers/anymal_controller.py
```